"""Pytest configuration for Task Planner backend tests."""
import pytest

# Tests run against the live local backend — make sure it's running on port 8001
# before running: uvicorn app.main:app --host 127.0.0.1 --port 8001
