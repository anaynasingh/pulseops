## Deferred

<!-- Format: - [date] item - trigger: <condition> -->

- [2026-06-25] /ai/chat date buckets (overdue / due-today / due-this-week / per-task labels) use the server date (`date.today()`), not the user's local date - trigger: shipping to users outside the server timezone, or first timezone-related "wrong day" report. Fix: send client local date/timezone in the `/ai/chat` request (frontend `api.ts` + `AIAssistantPanel` + backend `_ChatRequest`) and bucket from it. Source: assistant-task-prompts probe R1/R2 Codex MEDIUM.
- [2026-06-25] /ai/chat task context is one fixed relevance slice (cap 40), not intent-aware - trigger: users with very large backlogs, or if priority/due-date/blocked questions need more precise retrieval. Fix: branch retrieval by prompt intent. Source: assistant-task-prompts probe R2 + PENDING correction.
- [2026-06-26] Intake UI has no in-form assignee/owner picker — the backend default-to-creator + override contract exists, but intake/page.tsx never sends owner_id/assigned_to, so a confirming user cannot reassign ownership from the UI (only the API can). Trigger: a user wants to confirm an intake on someone else's behalf, or product asks for an in-form "assign to" affordance. Fix: add an assignee/owner picker to frontend/app/(dashboard)/intake/page.tsx (build the confirm body at page.tsx:58-72 to include the chosen owner_id/assigned_to) plus a users-list fetch in lib/api.ts; sending null/UUID flows through the contract that already exists. Source: intake-default-assignee plan AMBIGUITY 1 (Orchestrator deferred, backend-only this burst).
- [2026-06-26] Project.progress_pct is not recalculated on task creation (create_task AND intake task→existing) or deletion — only on task PATCH (update_task) when is_completed/status changes. A project at 100% stays shown complete after new todo tasks are added. Trigger: a "project shows complete but has open tasks" report, or when progress accuracy on board/dashboard/AI-context matters. Fix: extract the count-based recalc from tasks.py update_task into a shared helper and call it on task create/delete and intake confirm. Source: intake-functional probe round 2 Codex C3 (MEDIUM, pre-existing app-wide behaviour, out of intake burst scope).
- [2026-06-26] No frontend component test harness — vitest is configured but finds zero test files, so UI/rendering bursts are verified only by tsc + eslint + production build + manual click-through. Trigger: the next frontend burst whose correctness is not fully covered by tsc/lint/build (any behavioural or rendering change where a manual click-through is currently the only verification). Fix: add React Testing Library + jsdom on the existing vitest config; first tests should cover the patterns shipped without coverage (e.g. ClaudeSetupModal renders into document.body and centers). Source: guide-modal-center retrospective (second consecutive frontend burst verified by eye only).
- [2026-07-06] Anchor the team-member domain guard in GET /users/ (`backend/app/api/v1/users.py:24`). Current: `User.email.like("%prospect33.com%")` is an unanchored substring match — admits any address merely containing the string (e.g. `attacker@prospect33.com.evil.io`). Trigger: user records can be created from any external/self-serve signup path (not just seeded + SSO), OR the same substring-domain pattern is copied to a second query, OR any auth/tenant-isolation burst touches this file. Fix: `User.email.like("%@prospect33.com")` or an explicit endswith on the domain. Low risk today (creation restricted to seeded + SSO, which issues real @prospect33.com addresses). Source: ag-challenge round 1 (assignable-users-sso), Low / V2 — pre-existing, out of burst scope.
- [2026-07-06] Rotate/revoke `api_key` endpoint (e.g. `POST /auth/api-key/rotate`). The permanent, plaintext, un-revocable `User.api_key` now grants full REST access (mcp-longlived-apikey); today the only kill-switch is deactivating the user, which itself lags up to 5 min via `_user_cache`. Trigger: any real-world key leak, OR first external/partner MCP consumer, OR before this repo touches production client data. HIGHEST-priority security debt (financial-services). Bundle the mid-TTL `_user_cache` deactivation-staleness fix into the same burst. Source: mcp-longlived-apikey (Orchestrator-accepted risk, rotate explicitly deferred; Codex plan-challenge C2/C4).
- [2026-07-06] Repair the RED `backend/tests/test_regression.py` suite: `login()`/`anayna_token`/`stephen_token` fixtures POST to the deleted `/auth/login` (removed at SSO, commit `271707b`), so the whole suite 404s. Needs a post-SSO non-interactive JWT-for-tests strategy (seed a token via `security.create_access_token`, or a marker-gated seeded user as `TestApiKeyAuth` now does). Trigger: the next burst needing JWT-path regression coverage, OR wanting the suite green in CI. The api_key path is currently the ONLY green auth coverage; the JWT happy-path is uncovered (`# AMBIGUITY`). Source: mcp-longlived-apikey (Codex plan-challenge C1 discovery).
- [2026-07-06] Token-keyed cache for the api_key auth path. Every api_key request does one DB lookup (the `_user_cache` is keyed by user_id and unreadable before the key resolves a user). Trigger: MCP call volume grows enough that the per-request Supabase-Sydney lookup (~800ms-1s) is a measured latency problem. Note the security cost: a token-keyed cache holds raw api_keys in memory. Source: mcp-longlived-apikey (Codex plan-challenge C4).
- [2026-07-06] Backfill automated tests for the local stdio MCP server (`mcp-servers/pulseops/server.py`) — currently manual-only (no test harness for the stdio path). Trigger: next change to that server. Source: mcp-longlived-apikey Stream B.

- [2026-07-23] Unique guard for the per-user "Meeting Action Items" catch-all project. `_resolve_target_project` (proposed_tasks.py) is check-then-create with no constraint: two concurrent first-time confirms by one user create duplicate rows, after which every confirm's `scalar_one_or_none()` raises MultipleResultsFound → permanent 500 for that user. Fix: partial unique index on `projects(owner_id, title)` scoped to the catch-all title (008 migration + run_migrations mirror) + IntegrityError catch-and-refetch. Trigger: first observed duplicate/500, OR next migration burst touching `projects`. Source: transcript-intake-bell ag-challenge build R1 (Medium, steer: defer 2026-07-23).
- RESOLVED 2026-07-24 (fired live within a day: Graph 403 on an externally-organized meeting pinned two users' cursors; fixed by per-meeting + per-transcript isolation, commit on dev). Original: [2026-07-23] Poison-meeting isolation in the transcript poll. A PERMANENT per-meeting Graph 4xx raises out of `_poll_user` each tick and pins that user's cursor forever (silent full stall for that user). Fix: per-meeting try/except that logs + skips the meeting while letting the iteration complete (cursor advances past it); decide whether skipped meetings get a bounded retry ledger. Trigger: first observed stuck cursor in poll logs, OR next transcript-poll burst. Source: transcript-intake-bell ag-challenge build R1 (Medium, steer: defer 2026-07-23).
- [2026-07-23] Repo-wide env-driven TLS verification toggle. `verify=False` on all outbound httpx clients (ai_service, m365 server, graph_service, DB SSL ctx) is the documented corporate-proxy convention but is untenable for production client data: replace with a `TLS_VERIFY`/CA-bundle setting defaulting to verified, opt-out per env. Trigger: before this repo touches production client data, OR first deployment outside the corp-proxy environment. Source: transcript-intake-bell peer review finding 1 (steer: accept convention now, queue toggle 2026-07-23).
- [2026-07-23] Durable user↔transcript association for extraction-recovery fan-out. When extraction fails for >1h, only the ingesting user (or a user whose scan window still covers the meeting) recovers proposals — other attendees silently miss them (# AMBIGUITY in transcript_poll_service.py, accepted for v1). Fix direction: persist per-user pending-extraction markers at encounter time, or fan out on recovery via attendee matching. Trigger: first user report of missing proposals after an extraction outage, OR next transcript-poll burst. Source: transcript-intake-bell steer ruling 2026-07-23.
- [2026-07-23] Transcript-poll replica scale-out serialization. R1-1 serialization (module asyncio.Lock + APScheduler max_instances=1) only holds in a single-process deploy; it silently stops serializing under >1 Railway replica or uvicorn/gunicorn --workers. Fix: move the trigger to external Railway Cron hitting /internal/run-transcript-poll, or a Postgres advisory lock. Trigger: before enabling >1 replica or --workers on the backend service. Source: transcript-intake-bell R1-1 determination + retro 2026-07-23.
- [2026-07-23] Repair test_reminders (6 failures): AsyncMock children are AsyncMock on Py3.12, so result.scalars()/scalar_one_or_none() return coroutines. Fix: MagicMock result objects (see tests/test_transcript_poll.py::_result helper); bundle a shared conftest mock-session helper so every backend suite stops re-hitting the trap. Distinct from the deferred RED test_regression suite (deleted /auth/login). Trigger: next reminder burst, OR next backend-test burst. Source: transcript-intake-bell retro 2026-07-23.
