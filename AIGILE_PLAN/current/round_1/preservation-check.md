--- AIGILE_PLAN/current/round_0/plan.md	2026-07-22 07:17:10.326510622 +0000
+++ AIGILE_PLAN/current/round_1/plan.md	2026-07-22 09:00:49.521522408 +0000
@@ -23,10 +23,10 @@
   - New router `proposed_tasks.py`, `APIRouter(prefix="/proposed-tasks")`, registered in `main.py` under `PREFIX="/api/v1"`.
   - `GET /api/v1/proposed-tasks/?status=pending` → `List[ProposedTaskOut]` where `ProposedTaskOut = { id: UUID, transcript_id: UUID, meeting_title: str, meeting_date: date|null, title: str, description: str|null, priority: "low"|"medium"|"high"|"urgent", assignee_hint: str|null, status: "pending"|"accepted"|"dismissed", dedup_status: str|null, created_task_id: UUID|null, proposed_at: datetime }`.
   - `GET /api/v1/proposed-tasks/count` → `{ "pending": int }` (bell badge).
-  - `POST /api/v1/proposed-tasks/confirm` body `{ accepted_ids: UUID[], dismissed_ids: UUID[], project_id?: UUID|null }` → `{ created: int, skipped_duplicates: int, dismissed: int, tasks: TaskOut[], results: [{ proposed_id: UUID, outcome: "created"|"skipped_duplicate"|"dismissed"|"not_found"|"already_handled", task_id?: UUID }] }`. SEMANTICS (Orchestrator ruling 2026-07-22): both lists are EXPLICIT — proposals in `accepted_ids` are created, proposals in `dismissed_ids` are dismissed, and any pending proposal in NEITHER list remains `pending` (stays in the bell for later triage). Confirm never implicitly dismisses. Auth: `GET` endpoints `Depends(get_current_user)`; `POST /confirm` uses `require_writer` (creates real Task rows — same guard as `create_task`; Orchestrator ruling 2026-07-22). Session: confirm runs on the RLS-stamped `get_db_for_user` session (consistent with `create_task`); each endpoint scoped to `ProposedTask.user_id == current_user.id`.
+  - `POST /api/v1/proposed-tasks/confirm` body `{ accepted_ids: UUID[], dismissed_ids: UUID[], project_id?: UUID|null }` → `{ created: int, skipped_duplicates: int, dismissed: int, tasks: TaskOut[], results: [{ proposed_id: UUID, outcome: "created"|"skipped_duplicate"|"dismissed"|"not_found"|"already_handled", task_id?: UUID }] }`. SEMANTICS (Orchestrator ruling 2026-07-22): both lists are EXPLICIT — proposals in `accepted_ids` are created, proposals in `dismissed_ids` are dismissed, and any pending proposal in NEITHER list remains `pending` (stays in the bell for later triage). Confirm never implicitly dismisses. Auth: `GET` endpoints `Depends(get_current_user)`; `POST /confirm` uses `require_writer` (creates real Task rows — same guard as `create_task`; Orchestrator ruling 2026-07-22). Session: confirm runs on the RLS-stamped `get_db_for_user` session (consistent with `create_task`); each endpoint scoped to `ProposedTask.user_id == current_user.id`. Overlap guard (C7, Orchestrator ABSORB-PLAN 2026-07-22): any UUID present in BOTH `accepted_ids` and `dismissed_ids` → deterministic HTTP 422 listing the offending ids; negative-path test required.
   - `POST /api/v1/internal/run-transcript-poll` (header `X-Cron-Secret`) → `{ users_polled, transcripts_ingested, proposed_created }`.
-  - DB: new table `proposed_tasks` (columns: `id UUID PK, user_id UUID NOT NULL FK users(id) ON DELETE CASCADE, transcript_id UUID FK meeting_transcripts(id) ON DELETE CASCADE, meeting_title TEXT, meeting_date DATE, title VARCHAR(500) NOT NULL, description TEXT, priority prioritylevel DEFAULT 'medium', assignee_hint VARCHAR(255), status VARCHAR(20) NOT NULL DEFAULT 'pending', created_task_id UUID, dedup_status VARCHAR(20), dedup_existing_task_id UUID, proposed_at TIMESTAMPTZ DEFAULT now(), handled_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT now()`). NOTE: the Postgres enum type is `priority_level` (with underscore — defined in `database/schema.sql:15` as `CREATE TYPE priority_level AS ENUM (...)` and used by tasks/projects at `schema.sql:73,135`), NOT `prioritylevel`; the SQLAlchemy class name is `PriorityLevel` but the live DB type is `priority_level`. The `priority` column MUST be declared `priority priority_level DEFAULT 'medium'` in both `007_proposed_tasks.sql` and the mirrored `run_migrations()` — `prioritylevel` would raise `type "prioritylevel" does not exist` and abort cold-start migration (gate #1) and app boot. New columns on `meeting_transcripts`: `graph_transcript_id VARCHAR(255)`, `graph_meeting_id VARCHAR(255)`, `truncated BOOLEAN DEFAULT FALSE` (records head+tail truncation of long VTTs before extraction), plus `CREATE UNIQUE INDEX IF NOT EXISTS idx_meeting_transcripts_graph_transcript_id ON meeting_transcripts (graph_transcript_id) WHERE graph_transcript_id IS NOT NULL` — `graph_transcript_id` is the per-transcript dedup lookup key (gate #4) and would otherwise seq-scan; the partial index must be UNIQUE so concurrent polls on multiple replicas cannot both pass a check-then-insert and duplicate a transcript (the constraint is the dedup guarantee, not just an index; partial-WHERE form matches the established convention at `main.py:115/117`). New column on `users`: `m365_last_transcript_poll TIMESTAMPTZ`. All via `007_proposed_tasks.sql` (idempotent `IF NOT EXISTS`) AND mirrored verbatim into `main.py::run_migrations()` (the established startup auto-migration — same place migration 002's `suggested_item_type` ALTER lives, `main.py:123`).
-  - Implementation notes for builder: port `_meeting_id_from_join_url` (server.py:643-661) and `_clean_vtt` (server.py:618-640) verbatim into `graph_service.py`; port the calendarView call (server.py:544-576, `GET /me/calendarView` with `startDateTime/endDateTime/$select=...onlineMeeting.../$orderby/$top`) and transcript chain (server.py:691-716: `GET /me/onlineMeetings/{q}/transcripts` then `/transcripts/{tid}/content?$format=text/vtt`), but parameterise every `_graph_request` by a per-user access token instead of the module-global `_get_token()`. Acquire the per-user token via `msal.SerializableTokenCache()` loaded from `decrypt_str(user.m365_token_cache)` + `auth._msal_app(cache).acquire_token_silent(settings.M365_GRAPH_SCOPES, account=...)`; if `cache.has_state_changed`, re-`encrypt_str(cache.serialize())` and persist (mirrors `ai.py:751-763`). Dedup already-ingested transcripts against the partial UNIQUE index on `meeting_transcripts.graph_transcript_id`: the insert must be `INSERT ... ON CONFLICT DO NOTHING` (or catch `IntegrityError`, rollback, and skip) so that when two concurrent polls race, the loser of the race skips cleanly instead of duplicating the transcript and its downstream proposals — do NOT rely on a bare SELECT-then-INSERT check, which is not atomic across replicas. Only proceed to extraction/proposal creation when the transcript insert actually took (rows affected / no conflict). Cap the cleaned VTT text fed to `structured_completion` at ~24,000 chars (≈6k tokens): if longer, apply a head+tail truncation keeping the first 75% and last 25% of the budget (join the two slices with an elision marker) and set `truncated: true` on the `MeetingTranscript` row — a multi-hour VTT must not blow the <10s CHARTER criterion or the model context limit. Extraction reuses `structured_completion(TRANSCRIPT_SYSTEM, ..., response_model=_TranscriptAIOutput)` (import `_TranscriptAIOutput` from `ai.py`) — this is the <10s extraction path. Each returned `action_item` → one `ProposedTask` (status `pending`, `assignee_hint=item.get("owner")`). Extract `recalc_project_progress(project_id)` from `tasks.py:197-218` into a shared function in `tasks.py`; call it from `create_task`, `delete_task`, `update_task`, and the confirm path. RLS ruling (Orchestrator 2026-07-22, resolves the round-1 Critical): the recalc's COUNT+UPDATE must execute on a SERVICE-CONTEXT session (no RLS user ctx — `projects` has `FORCE ROW LEVEL SECURITY` and its UPDATE policy is owner/creator/admin-only, so an RLS-stamped session silently updates zero rows when the caller doesn't own the project). The helper opens its own non-RLS session (plain `AsyncSessionLocal`) internally regardless of the caller's session; `progress_pct` is derived aggregate data, not user-authored content. This also fixes the same latent cross-owner no-op in the existing `update_task` path. In the confirm path, do NOT call `recalc_project_progress` per-created-task inside the accept loop — collect the distinct `project_id`s of all accepted tasks and call `recalc_project_progress` exactly ONCE per affected project after the batch completes. Pre-add dedup = deterministic normalized-title compare (`lower()`, strip non-alphanumeric, collapse whitespace; exact-normalized match OR `difflib.SequenceMatcher ratio >= 0.9`) against existing `Task.title` in the resolved project — NOT pgvector (embeddings require a live HuggingFace API round-trip per call, too slow/flaky for the pre-add gate). Confirm path busts `_kanban_cache` (import from `projects`, as `ai.py:29` does). Add `apscheduler` to `requirements.txt` and start an `AsyncIOScheduler` in a `main.py` startup hook calling `run_transcript_poll` every ~10 min; also register the `internal` router and add `CRON_SECRET` to `config.py`.
+  - DB: new table `proposed_tasks` (columns: `id UUID PK, user_id UUID NOT NULL FK users(id) ON DELETE CASCADE, transcript_id UUID FK meeting_transcripts(id) ON DELETE CASCADE, meeting_title TEXT, meeting_date DATE, title VARCHAR(500) NOT NULL, description TEXT, priority prioritylevel DEFAULT 'medium', assignee_hint VARCHAR(255), status VARCHAR(20) NOT NULL DEFAULT 'pending', created_task_id UUID, dedup_status VARCHAR(20), dedup_existing_task_id UUID, proposed_at TIMESTAMPTZ DEFAULT now(), handled_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT now()`). NOTE: the Postgres enum type is `priority_level` (with underscore — defined in `database/schema.sql:15` as `CREATE TYPE priority_level AS ENUM (...)` and used by tasks/projects at `schema.sql:73,135`), NOT `prioritylevel`; the SQLAlchemy class name is `PriorityLevel` but the live DB type is `priority_level`. The `priority` column MUST be declared `priority priority_level DEFAULT 'medium'` in both `007_proposed_tasks.sql` and the mirrored `run_migrations()` — `prioritylevel` would raise `type "prioritylevel" does not exist` and abort cold-start migration (gate #1) and app boot. New columns on `meeting_transcripts`: `graph_transcript_id VARCHAR(255)`, `graph_meeting_id VARCHAR(255)`, `truncated BOOLEAN DEFAULT FALSE` (records head+tail truncation of long VTTs before extraction), plus `CREATE UNIQUE INDEX IF NOT EXISTS idx_meeting_transcripts_graph_transcript_id ON meeting_transcripts (graph_transcript_id) WHERE graph_transcript_id IS NOT NULL` — `graph_transcript_id` is the per-transcript dedup lookup key (gate #4) and would otherwise seq-scan; the partial index must be UNIQUE so concurrent polls on multiple replicas cannot both pass a check-then-insert and duplicate a transcript (the constraint is the dedup guarantee, not just an index; partial-WHERE form matches the established convention at `main.py:115/117`). Extraction-storage (C1 / whitelist item 2): the extracted action-items JSON is persisted on the EXISTING `meeting_transcripts.action_items` JSONB column (verified at `models.py:326` — a JSONB column already suitable for storing extracted action-items JSON), so NO new column is added for extraction storage — neither `007_proposed_tasks.sql` nor the `run_migrations()` mirror adds one for this. New column on `users`: `m365_last_transcript_poll TIMESTAMPTZ`. All via `007_proposed_tasks.sql` (idempotent `IF NOT EXISTS`) AND mirrored verbatim into `main.py::run_migrations()` (the established startup auto-migration — same place migration 002's `suggested_item_type` ALTER lives, `main.py:123`).
+  - Implementation notes for builder: port `_meeting_id_from_join_url` (server.py:643-661) and `_clean_vtt` (server.py:618-640) verbatim into `graph_service.py`; port the calendarView call (server.py:544-576, `GET /me/calendarView` with `startDateTime/endDateTime/$select=...onlineMeeting.../$orderby/$top`) and transcript chain (server.py:691-716: `GET /me/onlineMeetings/{q}/transcripts` then `/transcripts/{tid}/content?$format=text/vtt`), but parameterise every `_graph_request` by a per-user access token instead of the module-global `_get_token()`. Acquire the per-user token via `msal.SerializableTokenCache()` loaded from `decrypt_str(user.m365_token_cache)` + `auth._msal_app(cache).acquire_token_silent(settings.M365_GRAPH_SCOPES, account=...)`; if `cache.has_state_changed`, re-`encrypt_str(cache.serialize())` and persist (mirrors `ai.py:751-763`). Recurring-occurrence binding (C2, Orchestrator ABSORB-PLAN 2026-07-22): the poll iterates the calendarView event instances and, for each event occurrence, selects the meeting's transcripts by filtering `createdDateTime` to the occurrence's `start..start+24h` window — port the source helper's occurrence-date PARAMETER (the ported chain already accepts one; do NOT rely on the default-latest branch at `server.py:697-707`). Every distinct transcript id in the window is ingested (constraint dedup makes re-seeing safe), so a recurring series with transcripts on two dates yields two distinct transcript rows, each proposal set labeled with its own occurrence date. Dedup already-ingested transcripts against the partial UNIQUE index on `meeting_transcripts.graph_transcript_id`: the insert must be `INSERT ... ON CONFLICT DO NOTHING` (or catch `IntegrityError`, rollback, and skip) so that when two concurrent polls race, the loser of the race skips cleanly instead of duplicating the transcript and its downstream proposals — do NOT rely on a bare SELECT-then-INSERT check, which is not atomic across replicas. Per-user proposal-creation idempotency (C1, Orchestrator ABSORB-PLAN 2026-07-22): proposal creation is NO LONGER gated on the transcript insert winning. The transcript row stays globally constraint-backed (one shared `meeting_transcripts` row per Teams transcript via the partial UNIQUE on `graph_transcript_id`), but for each user's poll — once the transcript row exists (inserted by this poll OR previously by anyone) — check whether that user already has `proposed_tasks` rows for `(user_id, transcript_id)`; if none exist, run extraction-or-reuse and create THAT user's proposals. Extraction runs at most once per transcript: the extracted action-items JSON is persisted on the shared transcript row's existing `action_items` JSONB column (`models.py:326`), so a second user attending the same meeting reuses the stored extraction instead of re-calling GPT-4o. Result: two connected users attending the same meeting each get their own pending proposals from exactly one transcript row and at most one LLM extraction call. Rejected: `UNIQUE (user_id, graph_transcript_id)` — that duplicates transcript content per user and re-runs extraction N times. Cap the cleaned VTT text fed to `structured_completion` at ~24,000 chars (≈6k tokens): if longer, apply a head+tail truncation keeping the first 75% and last 25% of the budget (join the two slices with an elision marker) and set `truncated: true` on the `MeetingTranscript` row — a multi-hour VTT must not blow the <10s CHARTER criterion or the model context limit. Extraction reuses `structured_completion(TRANSCRIPT_SYSTEM, ..., response_model=_TranscriptAIOutput)` (import `_TranscriptAIOutput` from `ai.py`) — this is the <10s extraction path. Extraction timeout (C4, Orchestrator ABSORB-PLAN 2026-07-22): wrap the poll-path `structured_completion` call in `asyncio.wait_for(..., timeout=30)` (hard bound) inside a per-transcript `try/except`; on timeout or model failure the transcript is marked ingested-but-unextracted (no proposals, no stored `action_items`) and is retried on the next tick (the per-user proposal-absence + missing-stored-extraction checks make retry idempotent). An extraction failure is caught per-transcript and does NOT abort the batch or block cursor advance. Each returned `action_item` → one `ProposedTask` (status `pending`, `assignee_hint=item.get("owner")`). Extract `recalc_project_progress(project_id)` from `tasks.py:197-218` into a shared function in `tasks.py`; call it from `create_task`, `delete_task`, `update_task`, and the confirm path. RLS ruling (Orchestrator 2026-07-22, resolves the round-1 Critical): the recalc's COUNT+UPDATE must execute on a SERVICE-CONTEXT session (no RLS user ctx — `projects` has `FORCE ROW LEVEL SECURITY` and its UPDATE policy is owner/creator/admin-only, so an RLS-stamped session silently updates zero rows when the caller doesn't own the project). The helper opens its own non-RLS session (plain `AsyncSessionLocal`) internally regardless of the caller's session; `progress_pct` is derived aggregate data, not user-authored content. This also fixes the same latent cross-owner no-op in the existing `update_task` path. In the confirm path, do NOT call `recalc_project_progress` per-created-task inside the accept loop — collect the distinct `project_id`s of all accepted tasks and call `recalc_project_progress` exactly ONCE per affected project after the batch completes. Generalized recalc ordering (C8, Orchestrator ABSORB-PLAN 2026-07-22): every caller (`create_task`, `update_task`, `delete_task`, confirm) invokes `recalc_project_progress` AFTER its own commit so the helper's separate service-session observes the new row state; the helper is best-effort — it catches and logs its own exceptions and NEVER raises into a caller whose write already committed. Pre-add dedup = deterministic normalized-title compare (`lower()`, strip non-alphanumeric, collapse whitespace; exact-normalized match OR `difflib.SequenceMatcher ratio >= 0.9`) against existing `Task.title` in the resolved project — NOT pgvector (embeddings require a live HuggingFace API round-trip per call, too slow/flaky for the pre-add gate). Confirm path busts `_kanban_cache` (import from `projects`, as `ai.py:29` does). Poll cursor contract (C3, Orchestrator ABSORB-PLAN 2026-07-22): the per-tick scan window is `[max(m365_last_transcript_poll - 1h overlap, now - 7 days), now]`. `m365_last_transcript_poll` advances to the poll-start timestamp ONLY when that user's entire iteration completes without exception; any failure leaves the cursor unchanged (next tick rescans — idempotent via constraint dedup + the per-user proposal-absence check + stored-extraction reuse). Per-user failure isolation: one user's exception is caught and logged and skips cursor advance for that user only, never aborting the batch. Acceptance: partial failure preserves the cursor; a clean iteration advances it; rescan creates no duplicates. Add `apscheduler` to `requirements.txt` and start an `AsyncIOScheduler` in a `main.py` startup hook calling `run_transcript_poll` every ~10 min; also register the `internal` router and add `CRON_SECRET` to `config.py` as `CRON_SECRET: Optional[str] = None` (C6, Orchestrator ABSORB-PLAN 2026-07-22) — a falsy value must make the internal endpoints return 401 rather than crash app boot, so local/test envs boot with no cron secret configured.
 - Dependency: none.
 - Executor: claude
 
@@ -37,6 +37,7 @@
   - `frontend/components/layout/Header.tsx`
   - `frontend/components/layout/ProposedTasksBell.tsx`
   - `frontend/package.json`
+  - `frontend/package-lock.json`
   - `frontend/vitest.config.ts`
   - `frontend/vitest.setup.ts`
   - `frontend/components/layout/__tests__/ProposedTasksBell.test.tsx`
@@ -50,9 +51,9 @@
 1. `007_proposed_tasks.sql` is idempotent AND byte-mirrored into `main.py::run_migrations()`; cold start applies cleanly.
 2. Live `GET /proposed-tasks/`, `/count`, `POST /confirm` responses match the documented shapes; bell renders in Header top-right on every dashboard page and badge reflects pending count.
 3. Confirm creates real `Task` rows under the resolved project, runs pre-add normalized-title dedup (duplicates skipped, reported in `results`), recalculates `progress_pct` via the shared service-session helper (correct even when the confirming user does not own the project — cross-owner recalc test required), dismisses only explicitly-dismissed items (unchecked items remain pending — partial-triage test required), clears handled items from the bell, and busts `_kanban_cache` so the board updates without reload.
-4. Poll job: per-user token failure isolated (one user's expired token does not abort the batch); already-ingested transcripts deduped by `graph_transcript_id` — dedup is constraint-backed (partial UNIQUE index + `ON CONFLICT DO NOTHING`/`IntegrityError` skip), NOT a check-then-insert, so two concurrent replica polls cannot both insert the same transcript; refreshed MSAL cache re-encrypted + persisted.
-5. Extraction call completes <10s (measured on the `structured_completion` call).
-6. Contract compliance: case enumeration (happy / empty accepted+dismissed / malformed UUID / already-handled proposed task / missing project or unconnected M365) each has a negative-path test; `pytest` backend suite and `vitest` frontend suite green. Omit-vs-null contract is not triggered (confirm is an explicit-lists POST, not a partial-update PATCH); if a per-item PATCH is added, it must use `'field' in body.model_fields_set` with the symmetric test pair. Errexit contract applies only if a bash helper is added (none planned).
+4. Poll job: per-user token failure isolated (one user's expired token does not abort the batch); already-ingested transcripts deduped by `graph_transcript_id` — dedup is constraint-backed (partial UNIQUE index + `ON CONFLICT DO NOTHING`/`IntegrityError` skip), NOT a check-then-insert, so two concurrent replica polls cannot both insert the same transcript; refreshed MSAL cache re-encrypted + persisted; two users attending the same meeting each get their own proposals from one shared transcript row (per-user proposal delivery — C1).
+5. Extraction call completes <10s (measured on the `structured_completion` call); a simulated slow LLM call fails boundedly (30s `asyncio.wait_for`) and the batch continues.
+6. Contract compliance: case enumeration (happy / empty accepted+dismissed / malformed UUID / already-handled proposed task / missing project or unconnected M365 / overlapping accepted+dismissed ids (422) / no CRON_SECRET configured) each has a negative-path test; `pytest` backend suite and `vitest` frontend suite green. Omit-vs-null contract is not triggered (confirm is an explicit-lists POST, not a partial-update PATCH); if a per-item PATCH is added, it must use `'field' in body.model_fields_set` with the symmetric test pair. Errexit contract applies only if a bash helper is added (none planned).
 **Deferred to next Burst:** pgvector/embedding-based semantic dedup (deterministic normalized-title used now); real-time push/WebSocket delivery (fixed-interval poll used now); Railway Cron wiring/ops config for the poll trigger endpoint; adopting/removing the orphaned legacy `NotificationBell` + registering the dormant `notifications` router. The two bundled items (vitest harness, `progress_pct` shared helper) are IN scope this burst, not deferred.
 
 ---

# Preservation verdict (2026-07-22)
3 hunks, all AUTHORISED against round_0/change-request.md:
- Hunk 1 (confirm contract): overlap guard 422 — whitelist item 9.
- Hunk 2 (DB spec + implementation notes): extraction-storage on existing action_items JSONB (item 2 — Plan agent verified models.py:326, no new column needed); C2 occurrence binding (3); C1 per-user idempotency replacing insert-won gate (1, 11); C4 timeout (5); C8 recalc ordering (10); C3 cursor contract (4); C6 CRON_SECRET optional (8).
- Hunk 3 (Stream B scope + gates): package-lock.json (7); gate #4 per-user delivery (12); gate #5 bounded-slow-LLM (6); gate #6 overlap + no-CRON_SECRET cases (8, 9).
No unauthorised additions, deletions, or modifications.
