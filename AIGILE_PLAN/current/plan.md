## Burst Plan
**Goal:** Auto-ingest Teams meeting transcripts via Microsoft Graph (per-user delegated token), extract action items with GPT-4o into PROPOSED tasks, and surface them in a top-right dashboard bell where the user confirms/dismisses (with pre-add duplicate detection) to create real kanban tasks.
**CHARTER alignment:** Objective "Meeting Intelligence working: transcript upload → action items, decisions, tasks extracted"; Success Criterion "Meeting transcript → task extraction completes in <10 seconds"; Hard Spec "OpenRouter (GPT-4o) for all LLM inference" and "All schema changes via numbered migration files".
**Streams:** list (2 parallel, disjoint writers) — Stream A Backend, Stream B Frontend. Backend is intentionally kept single-stream because every backend sub-concern must write `models.py` + `main.py` (shared writers), so splitting it would create coordination points rather than disjoint streams.

### Stream A - Backend transcript ingestion, proposed-task store, poll job, and API
- Scope (files WRITTEN):
  - `backend/app/models/models.py`
  - `backend/app/schemas/schemas.py`
  - `backend/app/services/graph_service.py`
  - `backend/app/services/transcript_poll_service.py`
  - `backend/app/api/v1/proposed_tasks.py`
  - `backend/app/api/v1/internal.py`
  - `backend/app/api/v1/tasks.py`
  - `backend/app/core/config.py`
  - `backend/app/main.py`
  - `backend/requirements.txt`
  - `database/007_proposed_tasks.sql`
  - `backend/tests/test_proposed_tasks.py`
  - `backend/tests/test_transcript_poll.py`
- Excluded: all `frontend/**`; `backend/app/api/v1/ai.py` (read-only reuse only — import `TRANSCRIPT_SYSTEM`/`structured_completion` from `ai_service`, import `_kanban_cache`/`_log_activity` as existing modules already do; do NOT edit `ai.py`); `backend/app/services/ai_service.py` (reuse as-is); `mcp-servers/m365/server.py` (source to port FROM, not modified); `backend/app/services/reminder_service.py`.
- Exports (interface contract consumed by Stream B):
  - New router `proposed_tasks.py`, `APIRouter(prefix="/proposed-tasks")`, registered in `main.py` under `PREFIX="/api/v1"`.
  - `GET /api/v1/proposed-tasks/?status=pending` → `List[ProposedTaskOut]` where `ProposedTaskOut = { id: UUID, transcript_id: UUID, meeting_title: str, meeting_date: date|null, title: str, description: str|null, priority: "low"|"medium"|"high"|"urgent", assignee_hint: str|null, status: "pending"|"accepted"|"dismissed", dedup_status: str|null, created_task_id: UUID|null, proposed_at: datetime }`.
  - `GET /api/v1/proposed-tasks/count` → `{ "pending": int }` (bell badge).
  - `POST /api/v1/proposed-tasks/confirm` body `{ accepted_ids: UUID[], dismissed_ids: UUID[], project_id?: UUID|null }` → `{ created: int, skipped_duplicates: int, dismissed: int, tasks: TaskOut[], results: [{ proposed_id: UUID, outcome: "created"|"skipped_duplicate"|"dismissed"|"not_found"|"already_handled", task_id?: UUID }] }`. SEMANTICS (Orchestrator ruling 2026-07-22): both lists are EXPLICIT — proposals in `accepted_ids` are created, proposals in `dismissed_ids` are dismissed, and any pending proposal in NEITHER list remains `pending` (stays in the bell for later triage). Confirm never implicitly dismisses. Auth: `GET` endpoints `Depends(get_current_user)`; `POST /confirm` uses `require_writer` (creates real Task rows — same guard as `create_task`; Orchestrator ruling 2026-07-22). Session: confirm runs on the RLS-stamped `get_db_for_user` session (consistent with `create_task`); each endpoint scoped to `ProposedTask.user_id == current_user.id`. Overlap guard (C7, Orchestrator ABSORB-PLAN 2026-07-22): any UUID present in BOTH `accepted_ids` and `dismissed_ids` → deterministic HTTP 422 listing the offending ids; negative-path test required.
  - `POST /api/v1/internal/run-transcript-poll` (header `X-Cron-Secret`) → `{ users_polled, transcripts_ingested, proposed_created }`.
  - DB: new table `proposed_tasks` (columns: `id UUID PK, user_id UUID NOT NULL FK users(id) ON DELETE CASCADE, transcript_id UUID FK meeting_transcripts(id) ON DELETE CASCADE, meeting_title TEXT, meeting_date DATE, title VARCHAR(500) NOT NULL, description TEXT, priority prioritylevel DEFAULT 'medium', assignee_hint VARCHAR(255), status VARCHAR(20) NOT NULL DEFAULT 'pending', created_task_id UUID, dedup_status VARCHAR(20), dedup_existing_task_id UUID, proposed_at TIMESTAMPTZ DEFAULT now(), handled_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT now()`). NOTE: the Postgres enum type is `priority_level` (with underscore — defined in `database/schema.sql:15` as `CREATE TYPE priority_level AS ENUM (...)` and used by tasks/projects at `schema.sql:73,135`), NOT `prioritylevel`; the SQLAlchemy class name is `PriorityLevel` but the live DB type is `priority_level`. The `priority` column MUST be declared `priority priority_level DEFAULT 'medium'` in both `007_proposed_tasks.sql` and the mirrored `run_migrations()` — `prioritylevel` would raise `type "prioritylevel" does not exist` and abort cold-start migration (gate #1) and app boot. New columns on `meeting_transcripts`: `graph_transcript_id VARCHAR(255)`, `graph_meeting_id VARCHAR(255)`, `truncated BOOLEAN DEFAULT FALSE` (records head+tail truncation of long VTTs before extraction), `extracted_at TIMESTAMPTZ NULL` (extraction-state marker — R1-2, Orchestrator ABSORB-PLAN 2026-07-22: extraction success ⟺ `extracted_at IS NOT NULL`, so a zero-action-item success is representable as `extracted_at` set with `action_items=[]`; all retry/reuse checks key on `extracted_at`, never on `action_items` truthiness), plus `CREATE UNIQUE INDEX IF NOT EXISTS idx_meeting_transcripts_graph_transcript_id ON meeting_transcripts (graph_transcript_id) WHERE graph_transcript_id IS NOT NULL` — `graph_transcript_id` is the per-transcript dedup lookup key (gate #4) and would otherwise seq-scan; the partial index must be UNIQUE so concurrent polls on multiple replicas cannot both pass a check-then-insert and duplicate a transcript (the constraint is the dedup guarantee, not just an index; partial-WHERE form matches the established convention at `main.py:115/117`). Extraction-storage (C1 / whitelist item 2): the extracted action-items JSON is persisted on the EXISTING `meeting_transcripts.action_items` JSONB column (verified at `models.py:326` — a JSONB column already suitable for storing extracted action-items JSON), so NO new column is added for extraction storage — neither `007_proposed_tasks.sql` nor the `run_migrations()` mirror adds one for this. New column on `users`: `m365_last_transcript_poll TIMESTAMPTZ`. All via `007_proposed_tasks.sql` (idempotent `IF NOT EXISTS`) AND mirrored verbatim into `main.py::run_migrations()` (the established startup auto-migration — same place migration 002's `suggested_item_type` ALTER lives, `main.py:123`).
  - Implementation notes for builder: port `_meeting_id_from_join_url` (server.py:643-661) and `_clean_vtt` (server.py:618-640) verbatim into `graph_service.py`; port the calendarView call (server.py:544-576, `GET /me/calendarView` with `startDateTime/endDateTime/$select=...onlineMeeting.../$orderby/$top`) and transcript chain (server.py:691-716: `GET /me/onlineMeetings/{q}/transcripts` then `/transcripts/{tid}/content?$format=text/vtt`), but parameterise every `_graph_request` by a per-user access token instead of the module-global `_get_token()`. calendarView pagination contract (R1-4, Orchestrator ABSORB-PLAN 2026-07-22): the ported calendarView call MUST follow `@odata.nextLink` until exhausted (`$top` stays 50 per page); the pagination loop is mandatory and completes before any cursor advance, so a busy week's events are never silently truncated to the first page. Acquire the per-user token via `msal.SerializableTokenCache()` loaded from `decrypt_str(user.m365_token_cache)` + `auth._msal_app(cache).acquire_token_silent(settings.M365_GRAPH_SCOPES, account=...)`; if `cache.has_state_changed`, re-`encrypt_str(cache.serialize())` and persist (mirrors `ai.py:751-763`). Recurring-occurrence binding (C2, Orchestrator ABSORB-PLAN 2026-07-22): the poll iterates the calendarView event instances and, for each event occurrence, selects the meeting's transcripts by filtering `createdDateTime` to the occurrence's `start..start+24h` window — port the source helper's occurrence-date PARAMETER (the ported chain already accepts one; do NOT rely on the default-latest branch at `server.py:697-707`). Every distinct transcript id in the window is ingested (constraint dedup makes re-seeing safe), so a recurring series with transcripts on two dates yields two distinct transcript rows, each proposal set labeled with its own occurrence date. Dedup already-ingested transcripts against the partial UNIQUE index on `meeting_transcripts.graph_transcript_id`: the insert must be `INSERT ... ON CONFLICT DO NOTHING` (or catch `IntegrityError`, rollback, and skip) so that when two concurrent polls race, the loser of the race skips cleanly instead of duplicating the transcript and its downstream proposals — do NOT rely on a bare SELECT-then-INSERT check, which is not atomic across replicas. Per-user proposal-creation idempotency (C1, Orchestrator ABSORB-PLAN 2026-07-22): proposal creation is NO LONGER gated on the transcript insert winning. The transcript row stays globally constraint-backed (one shared `meeting_transcripts` row per Teams transcript via the partial UNIQUE on `graph_transcript_id`), but for each user's poll — once the transcript row exists (inserted by this poll OR previously by anyone) — check whether that user already has `proposed_tasks` rows for `(user_id, transcript_id)`; if none exist, run extraction-or-reuse and create THAT user's proposals. Extraction runs at most once per transcript: the extracted action-items JSON is persisted on the shared transcript row's existing `action_items` JSONB column (`models.py:326`) and `extracted_at` is stamped on success, so a second user attending the same meeting whose transcript row already has `extracted_at IS NOT NULL` reuses the stored extraction instead of re-calling GPT-4o — the reuse decision keys on `extracted_at IS NOT NULL`, never on `action_items` truthiness (R1-2). Result: two connected users attending the same meeting each get their own pending proposals from exactly one transcript row and at most one LLM extraction call. Rejected: `UNIQUE (user_id, graph_transcript_id)` — that duplicates transcript content per user and re-runs extraction N times. Cap the cleaned VTT text fed to `structured_completion` at ~24,000 chars (≈6k tokens): if longer, apply a head+tail truncation keeping the first 75% and last 25% of the budget (join the two slices with an elision marker) and set `truncated: true` on the `MeetingTranscript` row — a multi-hour VTT must not blow the <10s CHARTER criterion or the model context limit. Extraction reuses `structured_completion(TRANSCRIPT_SYSTEM, ..., response_model=_TranscriptAIOutput)` (import `_TranscriptAIOutput` from `ai.py`) — this is the <10s extraction path. Extraction timeout (C4, Orchestrator ABSORB-PLAN 2026-07-22): wrap the poll-path `structured_completion` call in `asyncio.wait_for(..., timeout=30)` (hard bound) inside a per-transcript `try/except`; on timeout or model failure the transcript is marked ingested-but-unextracted (`extracted_at` stays NULL, no proposals) and is retried on the next tick (the per-user proposal-absence + `extracted_at IS NULL` checks make retry idempotent). An extraction failure is caught per-transcript and does NOT abort the batch or block cursor advance. Window-independent retry sweep (R1-3, Orchestrator ABSORB-PLAN 2026-07-22): retry is decoupled from the calendar scan window — each tick, after the calendarView scan, re-pick `meeting_transcripts` rows where `extracted_at IS NULL AND created_at > now() - 7 days` and run extraction for them (per-user proposal fan-out follows the standard path), bounded by the 7-day age cap so a transcript first seen days earlier still gets re-extracted. Each returned `action_item` → one `ProposedTask` (status `pending`, `assignee_hint=item.get("owner")`). Extract `recalc_project_progress(project_id)` from `tasks.py:197-218` into a shared function in `tasks.py`; call it from `create_task`, `delete_task`, `update_task`, and the confirm path. RLS ruling (Orchestrator 2026-07-22, resolves the round-1 Critical): the recalc's COUNT+UPDATE must execute on a SERVICE-CONTEXT session (no RLS user ctx — `projects` has `FORCE ROW LEVEL SECURITY` and its UPDATE policy is owner/creator/admin-only, so an RLS-stamped session silently updates zero rows when the caller doesn't own the project). The helper opens its own non-RLS session (plain `AsyncSessionLocal`) internally regardless of the caller's session; `progress_pct` is derived aggregate data, not user-authored content. This also fixes the same latent cross-owner no-op in the existing `update_task` path. In the confirm path, do NOT call `recalc_project_progress` per-created-task inside the accept loop — collect the distinct `project_id`s of all accepted tasks and call `recalc_project_progress` exactly ONCE per affected project after the batch completes. Generalized recalc ordering (C8, Orchestrator ABSORB-PLAN 2026-07-22): every caller (`create_task`, `update_task`, `delete_task`, confirm) invokes `recalc_project_progress` AFTER its own commit so the helper's separate service-session observes the new row state; the helper is best-effort — it catches and logs its own exceptions and NEVER raises into a caller whose write already committed. Task-move recalc (R1-5, Orchestrator ABSORB-PLAN 2026-07-22): when `update_task` changes a task's `project_id`, it recalcs BOTH the old and the new project (post-commit, best-effort, per the C8 contract) so neither project's `progress_pct` is left stale. Pre-add dedup = deterministic normalized-title compare (`lower()`, strip non-alphanumeric, collapse whitespace; exact-normalized match OR `difflib.SequenceMatcher ratio >= 0.9`) against existing `Task.title` in the resolved project — NOT pgvector (embeddings require a live HuggingFace API round-trip per call, too slow/flaky for the pre-add gate). Confirm path busts `_kanban_cache` (import from `projects`, as `ai.py:29` does). Poll cursor contract (C3, Orchestrator ABSORB-PLAN 2026-07-22): the per-tick scan window is `[max(m365_last_transcript_poll - 1h overlap, now - 7 days), now]`. `m365_last_transcript_poll` advances to the poll-start timestamp ONLY when that user's entire iteration completes without exception; any failure leaves the cursor unchanged (next tick rescans — idempotent via constraint dedup + the per-user proposal-absence check + stored-extraction reuse). Per-user failure isolation: one user's exception is caught and logged and skips cursor advance for that user only, never aborting the batch. Acceptance: partial failure preserves the cursor; a clean iteration advances it; rescan creates no duplicates. Add `apscheduler` to `requirements.txt` and start an `AsyncIOScheduler` in a `main.py` startup hook calling `run_transcript_poll` every ~10 min; also register the `internal` router and add `CRON_SECRET` to `config.py` as `CRON_SECRET: Optional[str] = None` (C6, Orchestrator ABSORB-PLAN 2026-07-22) — a falsy value must make the internal endpoints return 401 rather than crash app boot, so local/test envs boot with no cron secret configured. Poll serialization contract (R1-1, Orchestrator ABSORB-PLAN 2026-07-22): serialize the poll per process — register the APScheduler job with `max_instances=1` AND guard both the scheduler callback and the `POST /internal/run-transcript-poll` handler with a shared module-level `asyncio.Lock`, so a manual internal-endpoint trigger during an active scheduled tick waits or returns HTTP 409 rather than running a second concurrent poll. Single-process deploy verified (Railway config, no `--workers`); replica scale-out stays Deferred (Railway Cron item). Rejected alternative (R1-1): a `UNIQUE (user_id, transcript_id, title)` constraint on `proposed_tasks` — title collisions within one transcript are legitimate (two distinct action items may share a short title), so a constraint would silently drop real proposals; serialization is the correct guard.
- Dependency: none.
- Executor: claude

### Stream B - Frontend proposed-task bell/panel + vitest test harness
- Scope (files WRITTEN):
  - `frontend/lib/api.ts`
  - `frontend/lib/types.ts`
  - `frontend/components/layout/Header.tsx`
  - `frontend/components/layout/ProposedTasksBell.tsx`
  - `frontend/package.json`
  - `frontend/package-lock.json`
  - `frontend/vitest.config.ts`
  - `frontend/vitest.setup.ts`
  - `frontend/components/layout/__tests__/ProposedTasksBell.test.tsx`
- Excluded: all `backend/**`; `frontend/components/layout/NotificationBell.tsx` (orphaned legacy reminder bell — do NOT repurpose or wire, out of scope); all `frontend/app/(dashboard)/*/page.tsx` (Header is imported by each page but takes the bell automatically once Header renders it — pages are NOT edited).
- Exports (interface contract): `proposedTasksApi` added to `lib/api.ts` with `list(status?)` → `GET /proposed-tasks/`, `count()` → `GET /proposed-tasks/count`, `confirm(accepted_ids, dismissed_ids, project_id?)` → `POST /proposed-tasks/confirm` (mirrors existing axios-wrapper style; note existing convention: collection GET uses no trailing-slash issues here since `/proposed-tasks/` has one). `ProposedTask` type in `types.ts` matching `ProposedTaskOut`. `ProposedTasksBell.tsx`: `Bell` icon (lucide-react, already a dep) in `Header.tsx` actions region top-right (before the AI Assistant button, line ~40); badge = pending count via `useQuery({queryKey:["proposed-tasks"]})`; query keys (R1-6, Orchestrator ABSORB-PLAN 2026-07-22): the badge count query uses `["proposed-tasks","count"]` and the panel list query uses `["proposed-tasks","list",status]` (superseding the bare `["proposed-tasks"]` shown for the badge above); all invalidations target the `["proposed-tasks"]` prefix so both refetch; the badge count query sets `refetchInterval: 60_000` (R1-8, matches the dashboard page precedent at `app/(dashboard)/dashboard/page.tsx:21`); dropdown panel groups items by `meeting_title`+`meeting_date`; checkboxes default checked; Confirm semantics (Orchestrator ruling 2026-07-22): "Add selected" → `confirm(checkedIds, [])` — unchecked items are NOT sent and remain pending in the bell for later triage; dismissal is a separate explicit affordance (per-item dismiss X and a "Dismiss unchecked" secondary button → `confirm([], dismissedIds)`); after any confirm call, `queryClient.invalidateQueries` for `["proposed-tasks"]`, `["projects-kanban"]`, `["my-dashboard"]`, `["dashboard"]` (kanban key verified at `KanbanBoard.tsx:59` as `["projects-kanban", ...]`; prefix invalidation matches). Vitest harness: add devDeps `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`, `@vitejs/plugin-react`; `vitest.config.ts` (`environment:'jsdom'`, setupFile, and `resolve.alias` mapping `'@'` → the repo `frontend` root to match the `tsconfig.json` `paths` `@/*` so `@/...` imports resolve under vitest — R1-7); `test` script in `package.json`. First tests cover bell badge count, panel grouping, default-checked state, "Add selected" sends only checked ids (unchecked stay pending, still rendered), explicit dismiss sends dismissed_ids, and empty-state.
- Dependency: none (builds against the API contract above; disjoint file set from Stream A). Integration-gated, not code-blocked.
- Executor: general-purpose-builder
- Executor rationale: disjoint frontend file set built in parallel with Stream A via a general-purpose Agent subagent (full tool scope needed for npm install + vitest runs); build-time provenance recorded 2026-07-23, supersedes the plan-time "claude" placeholder.

**Shared files:** none (Stream A writes only `backend/**` + `database/**`; Stream B writes only `frontend/**` — pairwise disjoint, verified).
**Integration gate:**
1. `007_proposed_tasks.sql` is idempotent AND byte-mirrored into `main.py::run_migrations()`; cold start applies cleanly.
2. Live `GET /proposed-tasks/`, `/count`, `POST /confirm` responses match the documented shapes; bell renders in Header top-right on every dashboard page and badge reflects pending count.
3. Confirm creates real `Task` rows under the resolved project, runs pre-add normalized-title dedup (duplicates skipped, reported in `results`), recalculates `progress_pct` via the shared service-session helper (correct even when the confirming user does not own the project — cross-owner recalc test required), dismisses only explicitly-dismissed items (unchecked items remain pending — partial-triage test required), clears handled items from the bell, and busts `_kanban_cache` so the board updates without reload.
4. Poll job: per-user token failure isolated (one user's expired token does not abort the batch); already-ingested transcripts deduped by `graph_transcript_id` — dedup is constraint-backed (partial UNIQUE index + `ON CONFLICT DO NOTHING`/`IntegrityError` skip), NOT a check-then-insert, so two concurrent replica polls cannot both insert the same transcript; refreshed MSAL cache re-encrypted + persisted; two users attending the same meeting each get their own proposals from one shared transcript row (per-user proposal delivery — C1); overlapping poll executions for one user (a manual internal trigger racing a scheduled tick) create no duplicate proposals — serialization test required (R1-1).
5. Extraction call completes <10s (measured on the `structured_completion` call); a simulated slow LLM call fails boundedly (30s `asyncio.wait_for`) and the batch continues.
6. Contract compliance: case enumeration (happy / empty accepted+dismissed / malformed UUID / already-handled proposed task / missing project or unconnected M365 / overlapping accepted+dismissed ids (422) / no CRON_SECRET configured / zero-action-item extraction success is terminal — `extracted_at` set, no proposals, no endless retry, R1-2) each has a negative-path test; `pytest` backend suite and `vitest` frontend suite green. Omit-vs-null contract is not triggered (confirm is an explicit-lists POST, not a partial-update PATCH); if a per-item PATCH is added, it must use `'field' in body.model_fields_set` with the symmetric test pair. Errexit contract applies only if a bash helper is added (none planned).
**Deferred to next Burst:** pgvector/embedding-based semantic dedup (deterministic normalized-title used now); real-time push/WebSocket delivery (fixed-interval poll used now); Railway Cron wiring/ops config for the poll trigger endpoint; adopting/removing the orphaned legacy `NotificationBell` + registering the dormant `notifications` router. The two bundled items (vitest harness, `progress_pct` shared helper) are IN scope this burst, not deferred.

---

### Ambiguities flagged (verified against code, contradict the burst prompt)

> ALL FOUR RESOLVED by Orchestrator rulings 2026-07-22: (1) APScheduler in-process + register internal router + CRON_SECRET — accepted; (2) vitest full harness — accepted (Stream B scope already reflects it); (3) project fallback chain (request → transcript project → auto-created per-user "Meeting Action Items") — accepted; (4) assignee = confirming user, assignee_hint display-only — accepted. Additional rulings: RLS service-session recalc (round-1 Critical), unchecked-stay-pending confirm semantics (round-1 High), require_writer on confirm (round-1 Medium).
1. **# AMBIGUITY — no APScheduler exists.** The prompt states "backend already runs apscheduler ... wired in main.py". Verified false: `apscheduler` is absent from `backend/requirements.txt` and `main.py`; the reminder job runs via `POST /internal/run-reminders` intended for external Railway Cron. Worse, that `internal` router is **not registered** in `main.py` and `internal.py` references `settings.CRON_SECRET` which **does not exist** in `config.py` — so reminders are currently dormant. Plan adds APScheduler in-process for the poll AND registers the `internal` router + adds `CRON_SECRET` (also un-dorming reminders as a low-cost side effect). Confirm this is acceptable vs. cron-only.
2. **# AMBIGUITY — vitest is not configured.** The prompt states "vitest is configured but has zero test files". Verified false: no `vitest` in `package.json` and no `vitest.config.*`. Stream B must add the full harness (vitest + RTL + jsdom + plugin-react), not just RTL/jsdom.
3. **# AMBIGUITY — accepted proposed tasks need a target project.** `Task.project_id` is NOT NULL, but the bell UX has no project picker. Plan resolves target project as: `confirm.project_id` if given → else `meeting_transcripts.project_id` → else an auto-created per-user catch-all "Meeting Action Items" project (mirrors the fallback in `ai.py::confirm_tasks`). Confirm this default, or specify that the panel must include a project selector.
4. **# AMBIGUITY — assignee for created tasks.** Existing transcript/chat task-creation paths default assignee to the creating user (`ai.py:598`, `ai.py:1461`). `assignee_hint` is a free-text name; plan defaults accepted-task `assigned_to` to `current_user` (not resolved from hint) to avoid `find_or_create_user_by_name` side effects during bulk accept. Confirm.

### Critical Files for Implementation
- /home/anayna/repos/pulseops/backend/app/models/models.py
- /home/anayna/repos/pulseops/backend/app/main.py
- /home/anayna/repos/pulseops/mcp-servers/m365/server.py
- /home/anayna/repos/pulseops/backend/app/api/v1/tasks.py
- /home/anayna/repos/pulseops/frontend/components/layout/Header.tsx
