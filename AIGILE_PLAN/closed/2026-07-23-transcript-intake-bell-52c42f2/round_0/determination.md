# Determination — round 0 Codex challenge (2026-07-22)

Orchestrator disposition: ABSORB all 8 findings into round-1 re-plan. No change to the two-stream decomposition.

| ID | Sev | Disposition | Determination |
|----|-----|-------------|---------------|
| C1 | HIGH (Codex Critical) | ABSORB-PLAN | Decouple per-user proposal creation from global transcript dedup. Transcript row insert stays constraint-backed (global partial UNIQUE on graph_transcript_id — one shared row per Teams transcript). Proposal creation becomes per-user idempotent: for each user's poll, if the transcript row exists (inserted by this poll OR previously by anyone) AND the user has no proposed_tasks rows for (user_id, transcript_id), run extraction-or-reuse and create that user's proposals. Extraction result is stored on the shared transcript row, so the second user reuses the stored extraction rather than re-calling GPT-4o. Acceptance: two connected users attending the same meeting each get their own pending proposals; exactly one transcript row; at most one LLM extraction call. Rejected alternative: UNIQUE (user_id, graph_transcript_id) on meeting_transcripts — duplicates transcript content per user and re-runs extraction N times. |
| C2 | HIGH | ABSORB-PLAN | Bind occurrence: the poll iterates calendarView event instances; for each event occurrence, transcript selection filters the meeting's transcripts by createdDateTime within the occurrence's start..start+24h window (ported helper already accepts an occurrence date — port that parameter, not just the default-latest branch). Every distinct transcript id in the window is ingested (constraint dedup makes re-seeing safe). Acceptance: recurring series with transcripts on two dates yields two distinct transcript rows, each proposal set labeled with its own occurrence date. |
| C3 | HIGH | ABSORB-PLAN | Poll cursor contract: window scanned = [max(m365_last_transcript_poll - 1h overlap, now - 7 days), now]. Cursor advances to the poll-start timestamp ONLY when the user's entire iteration completes without exception; any failure leaves the cursor unchanged (next tick rescans; idempotent via constraint dedup + per-user proposal check + stored-extraction reuse). Per-user failure isolation: one user's exception is caught, logged, skips cursor advance for that user only. Acceptance tests: partial failure preserves cursor; success advances it; rescan creates no duplicates. |
| C4 | HIGH | ABSORB-PLAN | Enforce <10s criterion boundedly: asyncio.wait_for 30s hard timeout around the structured_completion call in the poll path; per-transcript try/except so timeout/model failure marks that transcript ingested-but-unextracted (extraction retried next tick; cursor still advances only per C3 on clean iteration — extraction failure counts as an exception for cursor purposes NO — extraction failure is caught per-transcript and does NOT block the batch or cursor; the unextracted transcript is retried because proposal-existence check finds no proposals and stored extraction is absent). Gate #5 gains: simulated slow LLM call fails boundedly, batch continues. |
| C5 | MED | ABSORB-PLAN | Add `frontend/package-lock.json` to Stream B Scope (files WRITTEN). |
| C6 | MED | ABSORB-PLAN | `CRON_SECRET: Optional[str] = None` in config.py; falsy → internal endpoints return 401 (never crash boot). Acceptance: app boots with no env var; internal endpoint 401s. |
| C7 | MED | ABSORB-PLAN | Overlapping accepted_ids ∩ dismissed_ids → deterministic 422 with the offending ids listed. Negative-path test required. |
| C8 | MED | ABSORB-PLAN | Recalc contract generalized: every caller (create_task, update_task, delete_task, confirm) invokes recalc_project_progress AFTER its own commit; helper is best-effort — catches and logs its own exceptions, never raises into a caller whose write already committed. Acceptance per-caller: recalc observes the new row state. |

Rejected global alternative: none of the findings justify re-decomposition; all are contract/lifecycle pins expressible in plan text.
